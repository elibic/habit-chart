// The one place that names the repository holding this site.
//
// The hub reads tools.json from here, and the editor commits back to it. If
// the repository is ever renamed or moved, change these three lines and
// nothing else.
window.HUB_CONFIG = {
  owner: 'elibic',
  repo: 'tools',
  branch: 'main',
  dataPath: 'tools.json',
}
